__pycache__/
*.pyc
.venv/
venv/
node_modules/
.claude/

# Secrets — never commit. Use .env.example as the template.
.env

# A2 leftover: the SQLite database is created automatically on first run.
tasks.db
